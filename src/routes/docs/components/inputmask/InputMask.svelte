<!-- <script>
	import inputmask from 'inputmask/dist/inputmask.es6'

	let el
	let instance

	let mask = '9999'

	let value

	function bind(...deps) {
		instance = new inputmask.default({ mask })
	}

	$: bind(mask)
	$: if (el && instance) instance.mask(el)
</script>

<input bind:this={el} bind:value /> -->
