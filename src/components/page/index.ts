import Page from './Page.svelte'
import PageFooter from './PageFooter.svelte'
import PageHeader from './PageHeader.svelte'

export { PageHeader, PageFooter, Page }

export default { PageHeader, PageFooter, Page }
